$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: blueprint-env
description: EXAMPLE environment for the azureml-project-blueprint. Uses Docker + uv package manager
version: 1
build:
  path: .
